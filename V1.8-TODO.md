# ~~To-Do: v1.8 — Export, Upload Batch, Landing Page, Audit Log, Dashboard BPH~~
# SELESAI: v1.8 — Export, Upload Batch, Landing Page, Audit Log, Dashboard BPH

> Dibuat: 2026-06-13  
> **Selesai: 2026-06-13 ✓ — Semua fitur v1.8 telah diimplementasikan**

---

## Keputusan Arsitektur

| Fitur | Keputusan |
|---|---|
| Export | Tombol di `/admin/warga`, export sesuai filter aktif + opsi semua |
| Upload batch | Template pre-filled per gereja (download dari `/tentang`), upload di `/admin/warga` |
| Landing page | `/tentang` — publik tanpa login, konten app + download template |
| Audit log scope | Semua tabel: `fakta_warga`, `kelurahan`, `gereja`, `kelompok`, `user_profiles` |
| Dashboard BPH | `/bph` — ringkasan angka + bar chart perbandingan gereja + tabel ranking kelurahan |
| Role baru | `bph` — akses peta + `/bph` + `/tentang`, tidak bisa akses admin panel |

---

## FASE A — Persiapan Database & Dependency

### A.1 Schema Database
- [x] Buat tabel `audit_log` di VPS:
  ```sql
  CREATE TABLE audit_log (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    user_id UUID REFERENCES user_profiles(id) ON DELETE SET NULL,
    user_nama TEXT NOT NULL DEFAULT '',
    action TEXT NOT NULL CHECK (action IN ('INSERT', 'UPDATE', 'DELETE')),
    tabel TEXT NOT NULL,
    record_id TEXT NOT NULL,
    data_lama JSONB,
    data_baru JSONB
  );
  CREATE INDEX idx_audit_log_timestamp ON audit_log(timestamp DESC);
  CREATE INDEX idx_audit_log_tabel ON audit_log(tabel);
  GRANT ALL PRIVILEGES ON TABLE audit_log TO gkj_user;
  ```
- [x] Update CHECK constraint `role` di `user_profiles`: tambah `'bph'`
  ```sql
  ALTER TABLE user_profiles DROP CONSTRAINT user_profiles_role_check;
  ALTER TABLE user_profiles ADD CONSTRAINT user_profiles_role_check
    CHECK (role IN ('viewer', 'entry', 'superadmin', 'bph'));
  ```
- [x] Update `supabase_migration.sql` untuk dokumentasi (tambah dua SQL di atas)

### A.2 Dependency Baru
- [x] Install `xlsx` (SheetJS): `npm install xlsx`
- [x] Install `recharts`: `npm install recharts`
- [x] Update `package.json` tercatat

---

## FASE B — Role `bph`

### B.1 Type & Middleware
- [x] Update `lib/types.ts` — tambah `'bph'` ke union type `Role`
- [x] Update `proxy.ts` (middleware):
  - Role `bph`: boleh akses `/`, `/tentang`, `/bph`, `/changelog`
  - Role `bph`: redirect ke `/bph` kalau mencoba akses `/admin/*`
  - Jangan lupa: `/tentang` juga harus bisa diakses **tanpa login** (public route)

### B.2 UI Manajemen User
- [x] Update `/app/admin/users/page.tsx` — tambah opsi `'bph'` di dropdown role
- [x] Tampilkan label "BPH" di tabel daftar user (saat ini hanya viewer/entry/superadmin)

---

## FASE C — Audit Log

### C.1 Helper
- [x] Buat `lib/audit.ts`:
  ```ts
  export async function logAudit(params: {
    userId: string, userName: string,
    action: 'INSERT' | 'UPDATE' | 'DELETE',
    tabel: string, recordId: string,
    dataLama?: object | null, dataBaru?: object | null
  })
  ```
- [x] Fungsi query ke tabel `audit_log` via `postgres.js`

### C.2 Integrasi ke API Routes
- [x] `/api/gereja/route.ts` — POST (INSERT), PUT (UPDATE), DELETE
- [x] `/api/kelompok/route.ts` — POST, PUT, DELETE
- [x] `/api/kelurahan/route.ts` — POST, PUT, DELETE
- [x] `/api/warga/route.ts` — POST (INSERT/upsert), PUT (UPDATE), DELETE
- [x] `/api/users/route.ts` — POST (INSERT), PUT (UPDATE role/status)
- [x] Ambil `userId` dan `userName` dari JWT payload yang sudah diverifikasi

### C.3 API & UI
- [x] Buat `/api/audit-log/route.ts` — GET (Super Admin only), dengan query params: `tabel`, `dari`, `sampai`, `limit`, `offset`
- [x] Buat `/app/admin/audit-log/page.tsx`:
  - Tabel: timestamp, user, action, tabel, record_id, data lama/baru (collapsible)
  - Filter: per tabel, per rentang tanggal
  - Pagination
- [x] Tambah link "Audit Log" di navigasi admin layout (`/app/admin/layout.tsx`)

---

## FASE D — Export Excel/CSV

### D.1 API Export
- [x] Buat `/api/export/warga/route.ts` — GET:
  - Query params: `tahun`, `gereja_id` (opsional — kalau kosong export semua)
  - Format: `?format=xlsx` atau `?format=csv`
  - Return file download dengan header `Content-Disposition`
  - Kolom: `tahun, gereja, kelompok, kelurahan, kecamatan, kota_kab, provinsi, jumlah_warga`

### D.2 UI di Admin Warga
- [x] Tambah tombol "Export Excel" dan "Export CSV" di `/app/admin/warga/page.tsx`
- [x] Tombol export menggunakan filter yang sedang aktif (tahun + gereja)
- [x] Tambah opsi "Export semua" (tanpa filter)

---

## FASE E — Template + Upload Batch

### E.1 API Template per Gereja
- [x] Buat `/api/template/[gereja_id]/route.ts` — GET (public, tidak perlu login):
  - Generate file Excel dengan `xlsx`
  - Sheet 1 "Data Warga": kolom `tahun (isi)`, `kelurahan_kode (jangan diubah)`, `kelurahan_nama`, `kecamatan`, `kota_kab`, `kelompok_kode (jangan diubah)`, `kelompok_nama`, `jumlah_warga (isi)`
  - Pre-filled dengan semua kombinasi kelurahan × kelompok milik gereja tersebut
  - Sheet 2 "Petunjuk": instruksi pengisian
  - Nama file: `template-warga-[nama-gereja]-[tahun].xlsx`

### E.2 API Upload Batch
- [x] Buat `/api/upload/warga/route.ts` — POST (Entry Data & Super Admin):
  - Terima multipart/form-data dengan file Excel
  - Parse dengan `xlsx`
  - Validasi: cek kolom wajib ada, `kelurahan_kode` valid, `kelompok_id` valid, `jumlah_warga >= 0`
  - Return preview rows (valid + invalid) sebelum disimpan
- [x] Buat endpoint konfirmasi: POST `/api/upload/warga/confirm` — simpan semua row yang valid ke `fakta_warga` (upsert)

### E.3 UI Upload di Admin Warga
- [x] Tambah tombol "Upload Batch" di `/app/admin/warga/page.tsx`
- [x] Modal/drawer upload:
  1. Step 1: pilih gereja → link download template untuk gereja itu
  2. Step 2: upload file Excel
  3. Step 3: preview tabel — row valid (hijau) vs invalid (merah + alasan error)
  4. Step 4: tombol "Simpan X baris valid" → panggil confirm endpoint

---

## FASE F — Landing Page `/tentang`

### F.1 Update Middleware
- [x] Update `proxy.ts`: tambah `/tentang` ke daftar public routes (skip auth check)

### F.2 Buat Halaman
- [x] Buat `/app/tentang/page.tsx` — konten:
  - **Hero section**: nama aplikasi + tagline singkat
  - **Tentang aplikasi**: kegunaan, untuk siapa, bagaimana cara akses
  - **Fitur utama**: peta interaktif, laporan, admin panel
  - **Download template per gereja**: list semua gereja dengan tombol download template Excel masing-masing (fetch dari `/api/gereja`, download dari `/api/template/[gereja_id]`)
  - **Cara akses**: instruksi untuk gereja baru (hubungi admin klasis)
  - Link ke `/changelog` dan link login

### F.3 Navigasi
- [x] Tambah link "Tentang" di navbar/layout yang relevan (halaman utama `/`)

---

## FASE G — Dashboard BPH (`/bph`)

### G.1 API Dashboard
- [x] Buat `/api/dashboard/route.ts` — GET (semua user login):
  - Query params: `tahun` (default: tahun terbaru yang ada data)
  - Return:
    ```json
    {
      "tahun": 2026,
      "tahunTersedia": [2024, 2025, 2026],
      "totalWarga": 1234,
      "totalGereja": 8,
      "totalKelurahan": 45,
      "perGereja": [{ "gereja_id", "nama", "jumlah_warga", "jumlah_kelurahan" }],
      "rankingKelurahan": [{ "kelurahan", "kecamatan", "kota_kab", "total_warga" }],
      "trenPerTahun": [{ "tahun", "total_warga" }]
    }
    ```

### G.2 Halaman `/bph`
- [x] Buat `/app/bph/page.tsx`:
  - **Header**: selector tahun
  - **Kartu ringkasan** (3 kartu): Total Warga Klasis, Jumlah Gereja, Kelurahan Ter-data
  - **Bar chart horizontal** (Recharts): perbandingan jumlah warga per gereja, diurutkan terbesar
  - **Tabel ranking kelurahan**: top 20 kelurahan warga terbanyak (lintas semua gereja), kolom: no, kelurahan, kecamatan, kota/kab, total warga
  - Mobile-optimized: stack vertikal, chart scrollable horizontal
- [x] Tambah link ke `/bph` di navigasi halaman utama (untuk user role `bph` dan `superadmin`)

---

## FASE H — Finalisasi & Deploy

### H.1 Update Versi & Changelog
- [x] Update README.md:
  - Tambah `v1.8` ke tabel changelog
  - Pindahkan fitur v1.8 dari "Rencana Pengembangan" ke "Selesai"
  - Update tabel Role & Akses (tambah baris BPH)
  - Update tabel API Reference (tambah endpoint baru)
- [x] Update `/app/changelog/page.tsx` — tambah entry v1.8
- [x] Update versi di `package.json` ke `1.8.0`

### H.2 Git & Deploy VPS
- [x] Commit semua perubahan dengan pesan: `feat: v1.8 — export, upload batch, landing page /tentang, audit log, dashboard BPH`
- [x] Push ke GitHub
- [x] Deploy ke VPS:
  ```bash
  cd /var/www/gkj-klasis
  git pull
  npm install
  npm run build
  pm2 restart gkj-klasis
  ```
- [x] Jalankan SQL schema baru di VPS (audit_log + update role constraint)
- [x] Verifikasi live di https://klasis.purwandaru.com

---

## Estimasi Waktu per Fase

| Fase | Estimasi |
|---|---|
| A — Database & Dependency | 30 menit |
| B — Role `bph` | 30 menit |
| C — Audit Log (helper + integrasi + UI) | 2–3 jam |
| D — Export Excel/CSV | 1 jam |
| E — Template + Upload Batch | 2–3 jam |
| F — Landing Page `/tentang` | 1–2 jam |
| G — Dashboard BPH | 2–3 jam |
| H — Finalisasi & Deploy | 30 menit |
| **Total** | **~10–13 jam** |
